syntax = "proto3";
package ledger;

message TransactionParams {}

message Transaction {
    string id = 1;
    string timestamp = 2;
    double amount = 3;
}

service Ledger {
    rpc GetTransactions(TransactionParams) returns (stream Transaction) {}
}
